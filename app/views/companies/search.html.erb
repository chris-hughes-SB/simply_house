<h1>Search Results</h1>

<%= render("companies/search_form") %>

<ul>
  <% @companies.each do |company| %>
    <li>
      <%= link_to(company.title, company_path(company.company_number)) %>
    </li>
  <% end %>
</ul>
