<h1>Search Results</h1>

<%= render("companies/search_form") %>

<ul>
  <% @companies.each do |item| %>
    <li>
      <%= link_to(item["title"], company_path(item["company_number"])) %>
    </li>
  <% end %>
</ul>
